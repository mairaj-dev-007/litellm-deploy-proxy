global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'litellm'
    static_configs:
      - targets: ['litellm:4000']  # LiteLLM exposes /metrics

  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']
